<Window x:Class="ModScope.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="ModScope"
        MinWidth="1180"
        MinHeight="760"
        Width="1450"
        Height="900">
    <DockPanel>
        <Border DockPanel.Dock="Top" Padding="10" BorderBrush="#D0D7DE" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Margin="0,0,8,0" VerticalAlignment="Center" Text="URL" />
                    <TextBox x:Name="UrlTextBox" Grid.Column="1" MinWidth="360" Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Grid.Column="2" Margin="8,0,0,0" Padding="12,4" Click="NavigateButton_Click" Content="Navigate" />
                    <Button Grid.Column="3" Margin="6,0,0,0" Padding="12,4" Click="BackButton_Click" Content="Back" />
                    <Button Grid.Column="4" Margin="6,0,0,0" Padding="12,4" Click="ForwardButton_Click" Content="Forward" />
                    <Button Grid.Column="5" Margin="6,0,0,0" Padding="12,4" Click="ReloadButton_Click" Content="Reload" />
                </Grid>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="150" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Margin="0,0,6,0" VerticalAlignment="Center" Text="Instance" />
                    <TextBox Grid.Column="1" Text="{Binding InstanceName, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Grid.Column="2" Margin="12,0,6,0" VerticalAlignment="Center" Text="Profile" />
                    <TextBox Grid.Column="3" Text="{Binding ProfileName, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Grid.Column="4" Margin="12,0,6,0" Padding="10,4" Click="LoadFixtureButton_Click" Content="Use fixture" />
                    <Button Grid.Column="5" HorizontalAlignment="Left" Padding="10,4" Click="LoadSourceButton_Click" Content="Load source" />
                    <Button Grid.Column="6" Padding="10,4" Click="ObserveButton_Click" Content="Observe page" />
                </Grid>

                <Grid Grid.Row="2" Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Margin="0,0,6,0" VerticalAlignment="Center" Text="Instance root" />
                    <TextBox Grid.Column="1" Text="{Binding InstanceRootPath, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Grid.Column="2" Margin="12,0,6,0" VerticalAlignment="Center" Text="Profile path" />
                    <TextBox Grid.Column="3" Text="{Binding ProfilePath, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Grid.Column="4" Margin="12,0,6,0" VerticalAlignment="Center" Text="Mods path" />
                    <TextBox Grid.Column="5" Text="{Binding ModsPath, UpdateSourceTrigger=PropertyChanged}" />
                </Grid>
            </Grid>
        </Border>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Margin="8" BorderBrush="#D0D7DE" BorderThickness="1">
                <wv2:WebView2 x:Name="Browser" />
            </Border>

            <ScrollViewer Grid.Column="1" Margin="0,8,8,8" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="12,0,4,12">
                    <TextBlock FontSize="20" FontWeight="SemiBold" Text="Local context" />
                    <TextBlock Margin="0,6,0,0" Text="{Binding SessionSummary}" TextWrapping="Wrap" />
                    <TextBlock Margin="0,4,0,0" Text="{Binding ObservationSummary}" TextWrapping="Wrap" />

                    <GroupBox Margin="0,12,0,0" Header="MOD identity confirmation">
                        <StackPanel Margin="8">
                            <TextBlock Text="Page identity" />
                            <TextBox Margin="0,4,0,8" Text="{Binding CandidateIdentity, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock Text="Local MOD record" />
                            <ComboBox Margin="0,4,0,8"
                                      ItemsSource="{Binding Candidates}"
                                      SelectedItem="{Binding SelectedCandidate, Mode=TwoWay}"
                                      DisplayMemberPath="DirectoryName" />
                            <StackPanel Orientation="Horizontal">
                                <Button Padding="10,4" Click="ConfirmInstalledButton_Click" Content="Confirm installed" />
                                <Button Margin="8,0,0,0" Padding="10,4" Click="ConfirmNotInstalledButton_Click" Content="Confirm not installed" />
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Margin="0,12,0,0" Header="Local awareness">
                        <StackPanel Margin="8">
                            <TextBlock FontWeight="SemiBold" Text="Status" />
                            <TextBlock Margin="0,2,0,6" Text="{Binding LocalContext.Status}" />
                            <TextBlock FontWeight="SemiBold" Text="Profile" />
                            <TextBlock Margin="0,2,0,6" Text="{Binding LocalContext.ProfileName}" />
                            <TextBlock FontWeight="SemiBold" Text="Enabled" />
                            <TextBlock Margin="0,2,0,6" Text="{Binding LocalContext.EnabledState}" />
                            <TextBlock FontWeight="SemiBold" Text="Priority" />
                            <TextBlock Margin="0,2,0,6" Text="{Binding LocalContext.Priority}" />
                            <TextBlock FontWeight="SemiBold" Text="Known version" />
                            <TextBlock Margin="0,2,0,6" Text="{Binding LocalContext.KnownVersion}" />
                            <TextBlock FontWeight="SemiBold" Text="Evidence" />
                            <ItemsControl Margin="0,2,0,6" ItemsSource="{Binding LocalContext.Evidence}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,0,0,2">
                                            <TextBlock Text="{Binding Kind}" />
                                            <TextBlock FontSize="11" Foreground="#57606A" Text="{Binding Source.RelativePath}" TextWrapping="Wrap" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock FontWeight="SemiBold" Text="Uncertainty" />
                            <ItemsControl Margin="0,2,0,0" ItemsSource="{Binding LocalContext.Uncertainties}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Margin="0,0,0,2" Text="{Binding}" TextWrapping="Wrap" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock Margin="0,6,0,0" FontWeight="SemiBold" Text="Diagnostics" />
                            <ItemsControl Margin="0,2,0,0" ItemsSource="{Binding LocalContext.Diagnostics}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Code}" />
                                            <TextBlock Text=": " />
                                            <TextBlock Text="{Binding Message}" TextWrapping="Wrap" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </GroupBox>

                    <Expander Margin="0,12,0,0" IsExpanded="True" Header="Inspector">
                        <StackPanel Margin="8">
                            <TextBlock FontWeight="SemiBold" Text="Directory" />
                            <TextBlock Margin="0,2,0,8" Text="{Binding Inspector.DirectoryName}" />
                            <TextBlock FontWeight="SemiBold" Text="Display name" />
                            <TextBlock Margin="0,2,0,8" Text="{Binding Inspector.ModInfo.DisplayName}" />
                            <TextBlock FontWeight="SemiBold" Text="Files" />
                            <ListBox Margin="0,4,0,8" MaxHeight="150" ItemsSource="{Binding Inspector.Files}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding RelativePath}" TextWrapping="Wrap" />
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            <TextBlock FontWeight="SemiBold" Text="XML references" />
                            <ListBox Margin="0,4,0,8" MaxHeight="150" ItemsSource="{Binding Inspector.XmlFiles}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,0,0,4">
                                            <TextBlock FontWeight="SemiBold" Text="{Binding RelativePath}" TextWrapping="Wrap" />
                                            <TextBlock FontSize="11" Text="{Binding ParseStatus}" />
                                            <ItemsControl ItemsSource="{Binding XPathCandidates}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Margin="8,0,0,0" FontSize="11" TextWrapping="Wrap">
                                                            <Run Text="XPath: " />
                                                            <Run Text="{Binding RawValue}" />
                                                        </TextBlock>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                            <ItemsControl ItemsSource="{Binding RawObservations}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Margin="8,0,0,0" Orientation="Horizontal">
                                                            <TextBlock FontSize="11" Text="Raw: " />
                                                            <TextBlock FontSize="11" Text="{Binding ElementName}" />
                                                            <TextBlock FontSize="11" Text=" (" />
                                                            <TextBlock FontSize="11" Text="{Binding ElementPath}" />
                                                            <TextBlock FontSize="11" Text=")" />
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            <TextBlock FontWeight="SemiBold" Text="Diagnostics" />
                            <ItemsControl Margin="0,4,0,8" ItemsSource="{Binding Inspector.Diagnostics}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Code}" />
                                            <TextBlock Text=": " />
                                            <TextBlock Text="{Binding Message}" TextWrapping="Wrap" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock FontWeight="SemiBold" Text="Page content preview" />
                            <TextBox Margin="0,4,0,0" MaxHeight="140" IsReadOnly="True" Text="{Binding PageContentPreview, Mode=OneWay}" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" />
                        </StackPanel>
                    </Expander>

                    <TextBlock Margin="0,12,0,0" Foreground="#57606A" Text="{Binding StatusMessage}" TextWrapping="Wrap" />
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </DockPanel>
</Window>
