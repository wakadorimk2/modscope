# ModScope Tech Stack

- No implementation language or framework is selected in the current design phase.
- The tracked project currently contains Markdown design documents and Serena configuration.
- No package manifest, dependency declaration, build runner, test runner, or formatter is defined.
- `.serena/project.yml` currently declares no project language.
- Do not infer an implementation stack from the design documents.
- Do not create a manifest or dependency during design work.
- Implementation-agnostic decisions are in `docs/design.md`.
